[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "personal-ai-core"
dynamic = ["version"]
description = "Core contracts and schemas for Personal AI"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
keywords = []
authors = [
  { name = "Your Name", email = "your.email@example.com" },
]
dependencies = [
  "pydantic>=2.5.0",
]

[tool.hatch.version]
path = "personal_ai_core/__init__.py"

